@using VendorPage.Models;
@{
  Layout = "_Layout";
}

<h1>Vendors Page</h1>

@if (@Model.Count == 0)
{
  <h3>There are no vendor profiles yet.</h3>
}

@foreach (Vendor vendor in Model)
{
  <h3><a href='/vendors/@vendor.Id'>@vendor.Name</a></h3>
}

<p><a href='/'>Home Page</a></p>